import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  itemWrapper: {
    padding: 10,
    borderWidth: 1,
    borderRadius: 3,
    borderColor: '#f2f2f2',
  },
  row: {
    flexDirection: 'row',
  },
  smallColumn: {
    width: 80,
  },
  simpleFlex: {
    flex: 1,
  },
});

export default styles;
